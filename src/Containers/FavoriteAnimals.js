/* import React from 'react'
import AnimalFCard from '../Components/AnimalFCard'

class FavoriteAnimals extends React.Component {
    

    render() {
        return (
            <div>
                <h1>Favorite Animals</h1>
                {this.props.animals.map(animalObj => < AnimalFCard key={animalObj.id} animal={animalObj} clickHandler={this.props.unfavoriteHandler}/>)}
            </div>
        )
    }
}

export default FavoriteAnimals */